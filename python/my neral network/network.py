class Network:

    def __init__(self, layers):
        self.layers = layers

        for i in range(len(self.layers)):
            for j in range(int(self.layers[i][0])):
                self.layers[i].append()

    def run(self):
        for
